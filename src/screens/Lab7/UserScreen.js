import React from 'react';
import { View,Text } from 'react-native';

const User = () =>{
    return(
        <View style={{flex:1, alignItems:'center',justifyContent:'center'}}>
            <Text style={{fontSize:40,color:'#006600'}}>UserScreen</Text>
        </View>
    );
};
export default User;